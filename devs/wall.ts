/// <reference path="gameElement.ts" />

class Wall extends GameElement{

    constructor(){
        super(window.innerWidth/2, window.innerHeight, "wall")
    }

    // publiv getRectangle(){

    // }
}